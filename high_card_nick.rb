#psuedocode

# build a deck
  # initialize an empty deck
  # loop through ranks then loop through suits
    # use .each_with_index to pull the worth of a card
      # loop suits
        # then push each pair into the deck array
